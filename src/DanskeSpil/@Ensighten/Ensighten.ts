export const pushGaEvent = (eventData: any) => {
  // Mock implementation - you can add console.log for debugging
  console.log('Mock pushGaEvent called with:', eventData);
};

export const pushGaEventOnPageRedirect = (eventData: any) => {
  // Mock implementation
  console.log('Mock pushGaEventOnPageRedirect called with:', eventData);
};
