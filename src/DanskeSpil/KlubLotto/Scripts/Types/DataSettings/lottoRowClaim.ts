import { SitecoreImage } from '../sitecore';

export type LottoRowClaimDataSettings = {
  claimCtaLabel: string;
  claimDisclaimer: string;
  claimImage: SitecoreImage;
  claimText: string;
  claimTitle: string;
  expiredImage: SitecoreImage;
  expiredTitle: string;
  receiptDisclaimer: string;
  receiptText: string;
  receiptTitle: string;
  redeemedImage: SitecoreImage;
  redeemedTitle: string;
}